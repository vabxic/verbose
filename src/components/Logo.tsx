import { Text } from "@jamsr-ui/react";

export const Logo = () => {
  return (
    <div className="flex items-center gap-2">
      <Text variant="h5" className="logo-text tracking-wide">
        VERBOSE
      </Text>
    </div>
  );
};
