import * as react_jsx_runtime from 'react/jsx-runtime';
import { Table } from '@tanstack/react-table';

type PaginationProps<T> = {
    table: Table<T>;
    take: number;
    className?: string;
    classNames?: {
        root?: string;
        pagesWrapper?: string;
    };
};
declare const Pagination: <T>({ table, take, className, classNames, }: PaginationProps<T>) => react_jsx_runtime.JSX.Element;

export { Pagination, type PaginationProps };
