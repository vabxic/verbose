import * as react from 'react';

/**
 * Merges an array of refs into a single memoized callback ref or `null`.
 */
declare const useMergeRefs: <Instance>(refs: Array<react.Ref<Instance> | undefined>) => react.RefCallback<Instance> | null;

export { useMergeRefs };
