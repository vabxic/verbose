import * as react_jsx_runtime from 'react/jsx-runtime';
import { ComponentPropsWithAs } from '@jamsr-ui/utils';

type Props = {
    heading: React.ReactNode;
    value: string;
    startContent?: React.ReactNode;
    endContent?: React.ReactNode;
    isDisabled?: boolean;
};
type TabProps<T extends React.ElementType = "button"> = ComponentPropsWithAs<T, Props>;
declare const Tab: <T extends React.ElementType = "button">($props: TabProps<T>) => react_jsx_runtime.JSX.Element;

export { Tab, type TabProps };
