import * as react_jsx_runtime from 'react/jsx-runtime';
import React from 'react';

type Props = {
    repeat: number;
    children: ((_: {
        position: number;
        index: number;
    }) => React.ReactNode) | React.ReactNode;
};
declare const Repeater: (props: Props) => react_jsx_runtime.JSX.Element[];

export { Repeater };
