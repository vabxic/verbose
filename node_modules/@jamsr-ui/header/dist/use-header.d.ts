import * as react from 'react';
import { UIProps, PropGetter } from '@jamsr-ui/utils';
import { HeaderVariantProps } from './style.js';
import 'tailwind-variants';
import 'tailwind-variants/dist/config.js';

type Props = HeaderVariantProps & {
    hideOnScroll?: boolean;
    isBordered?: boolean;
    visibleBound?: number;
};
type UseHeaderProps = UIProps<"header", Props>;
declare const useHeader: ($props: UseHeaderProps) => {
    isBordered: boolean;
    hideOnScroll: boolean;
    children: react.ReactNode;
    getBaseProps: PropGetter;
    isVisible: boolean;
};

export { type UseHeaderProps, useHeader };
