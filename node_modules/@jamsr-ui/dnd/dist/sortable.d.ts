import { UniqueIdentifier } from '@dnd-kit/core';
import * as react_jsx_runtime from 'react/jsx-runtime';
import { SortableItemProps } from './sortable-item.js';
import 'react';

type Props<T> = {
    value: T[];
    onValueChange?: (item: T[]) => void;
    children: (_: SortableItemProps & {
        item: T;
    }) => React.ReactNode;
    isDisabled?: boolean;
};
declare const Sortable: <T extends {
    id: UniqueIdentifier;
}>(props: Props<T>) => react_jsx_runtime.JSX.Element;

export { Sortable };
