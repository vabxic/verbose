import * as react_jsx_runtime from 'react/jsx-runtime';
import { Placement } from '@floating-ui/react';
import { SlotsToClasses } from '@jamsr-ui/utils';
import { PopoverVariantProps, PopoverSlots } from './styles.mjs';
import 'tailwind-variants';
import 'tailwind-variants/dist/config.js';

type PopoverProps = PopoverVariantProps & {
    classNames?: SlotsToClasses<PopoverSlots>;
    children: React.ReactNode;
    trigger: React.JSX.Element;
    initialOpen?: boolean;
    placement?: Placement;
    isModal?: boolean;
    isOpen?: boolean;
    onOpenChange?: (open: boolean) => void;
    enabled?: boolean;
    triggerOn?: "click" | "hover";
    showArrow?: boolean;
    className?: string;
    offset?: number;
    lockScroll?: boolean;
};
declare const Popover: ($props: PopoverProps) => react_jsx_runtime.JSX.Element;

export { Popover, type PopoverProps };
