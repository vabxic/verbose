import * as react from 'react';
import * as _jamsr_ui_utils from '@jamsr-ui/utils';
import { UIProps, PropGetter } from '@jamsr-ui/utils';
import { ButtonVariantProps } from './styles.js';
import 'tailwind-variants';
import 'tailwind-variants/dist/config.js';

interface ButtonCustomProps {
}
type Props = ButtonCustomProps & UIProps<"button"> & {
    startContent?: React.ReactNode;
    endContent?: React.ReactNode;
    isLoading?: boolean;
    isDisabled?: boolean;
    spinnerPlacement?: "start" | "end";
    disableRipple?: boolean;
    disableAnimation?: boolean;
    ref?: React.Ref<HTMLButtonElement>;
    spinner?: React.ReactNode;
    isFormControl?: boolean;
};
type UseButtonProps = Props & ButtonVariantProps;
declare const useButton: ($props: UseButtonProps) => {
    Component: _jamsr_ui_utils.As<any>;
    spinner: react.ReactNode;
    children: react.ReactNode;
    styles: string;
    startContent: react.ReactNode;
    endContent: react.ReactNode;
    isLoading: boolean;
    spinnerPlacement: "start" | "end";
    isDisabled: boolean;
    disableRipple: boolean | undefined;
    getButtonProps: PropGetter;
};

export { type ButtonCustomProps, type UseButtonProps, useButton };
