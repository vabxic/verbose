import * as react_jsx_runtime from 'react/jsx-runtime';
import { FileUploadSingleProps } from '@jamsr-ui/file-upload-single';
import { Editor } from '@tiptap/react';

type ImageUploadProps = Omit<FileUploadSingleProps, "onFileSelect"> & {
    onFileSelect: (file: File) => Promise<{
        src: string;
        width: number;
        height: number;
        alt: string;
        placeholder?: string;
    }>;
};
declare const ImageUploadComponent: ({ getPos, editor, imageUploadProps, }: {
    getPos: () => number;
    editor: Editor;
    imageUploadProps: ImageUploadProps;
}) => react_jsx_runtime.JSX.Element;

export { ImageUploadComponent, type ImageUploadProps };
