import * as react_jsx_runtime from 'react/jsx-runtime';
import * as _jamsr_ui_utils from '@jamsr-ui/utils';
import { UIProps, PropGetter } from '@jamsr-ui/utils';
import { AvatarVariants } from './styles.js';
import 'tailwind-variants';
import 'tailwind-variants/dist/config.js';

interface Props {
    name?: string;
    fallback?: string | ((_: {
        alt: string;
        name?: string;
    }) => string);
    src?: null | string;
    alt: string;
    classNames?: {
        base?: string;
        image?: string;
        fallbackIcon?: string;
    };
}
type UseAvatarProps = UIProps<"img", Props> & AvatarVariants;
declare const useAvatar: ($props: UseAvatarProps) => {
    Component: _jamsr_ui_utils.As<any>;
    imgSrc: string | null | undefined;
    fallBack: string | react_jsx_runtime.JSX.Element;
    getBaseProps: PropGetter;
    getImageProps: PropGetter;
};

export { type UseAvatarProps, useAvatar };
